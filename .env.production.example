# Production (AlwaysData)
NODE_ENV=production
PORT=4000

# PostgreSQL (AlwaysData)
# Remplacez YOUR_PASSWORD et verifiez le port dans l'interface AlwaysData.
DATABASE_URL="postgresql://myconcretelab:YOUR_PASSWORD@postgresql-myconcretelab.alwaysdata.net:5432/myconcretelab_contrats?schema=public"
# Si SSL est requis:
# DATABASE_URL="postgresql://myconcretelab:YOUR_PASSWORD@postgresql-myconcretelab.alwaysdata.net:5432/myconcretelab_contrats?schema=public&sslmode=require"

# Optionnel
# CLIENT_DIST_DIR=/home/myconcretelab/contrats/client/dist
# NPM_INSTALL_MODE=install  # ./update: "ci" (defaut, propre/strict) ou "install" (garde node_modules)
# PLAYWRIGHT_BROWSERS_PATH=/home/myconcretelab/.cache/ms-playwright
# Evitez "~" dans les fichiers .env: dotenv ne l'expanse pas toujours.
# PLAYWRIGHT_BROWSERS_PATH=0  # stocke dans node_modules (souvent retelecharge apres npm ci)
# DATA_DIR=/home/myconcretelab/contrats/server/data
# PDF_SUBDIR=pdfs
# BASIC_AUTH_PASSWORD=

# Redemarrage auto du serveur dans ./update
# Option 1: commande de restart
# RESTART_CMD="touch /home/myconcretelab/www/votresite/tmp/restart.txt"
#
# Option 2: API Alwaysdata
# ALWAYSDATA_API_TOKEN=your_api_token
# ALWAYSDATA_ACCOUNT=myconcretelab
# ALWAYSDATA_SITE_ID=123456
